spring:
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

spring:
  boot:
    admin:
      url: http://localhost:9090